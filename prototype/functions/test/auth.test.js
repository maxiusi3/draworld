})  })

    })
d')is requirer ID setoThrow('Us.ct).rejeata })ata: mockDped({ dpect(wrapawait ex         
   rofile)
(updateUserP test.wrap =t wrapped   cons{}

   = t mockData ons   c
   () => {, async s'uired fields reqatelid
    it('va
    })
rue }) tss:al({ succetoEqult).resu  expect()
          }y(Object),
xpect.anpdatedAt: e   u',
     ated Nameme: 'UpdNa   display   ({
  enCalledWithe).toHaveBeDoc.updatockt(m expec

     ata })mockDta:  da({wrappedit ult = awaconst resle)
      erProfiUstepda(utest.wrap wrapped =       const

oc)alue(mockDckReturnVoc.molection().drestore.colmockFi   }
      fn(),
   t.e: jes   updat    {
 ockDoc = onst m

      c      } Name',
e: 'UpdatedlayNamsp       did',
 est-user-i  userId: 't      ta = {
st mockDacon{
      nc () => ata', asye dfilroer pates uspdt('u   i
 > {ile', () =serProfateUribe('upd)

  desc }
    })
   )  })
    l,
      : nulplayName    dis     ({
 ctContainingje expect.obh(
       WitCalledeBeentoHavoc.set).t(mockD  expec

    r)mockUseed(it wrapp)
      awafileserProap(createUtest.wrd = appewrnst    co)

   ue(mockDocurnValdoc.mockRetection().e.colltorockFires  m    }
    t.fn(),
  set: jes{
        Doc = mockconst 

      
      }.com',xampleest@e: 't       emailr-id',
 st-use    uid: 'ter = {
    t mockUse    cons=> {
  sync () y name', aspla diles missing it('hand
   })
    })
,
      y(Object) expect.antLoginAt:     las
   (Object),t.anyAt: expecreated   c0,
     s: 15    credit   
 r',UseName: 'Test  display      com',
 st@example.  email: 'te     id',
 est-user-d: 't       idWith({
 alleHaveBeenCtot).se(mockDoc.     expect
 r)
d(mockUseppeit wrawa     a
 serProfile)eU.wrap(creat= testt wrapped 
      consoc)
(mockDReturnValuen().doc.mocke.collectioockFirestor}
      mn(),
      set: jest.f     Doc = {
     const mock   }

        User',
me: 'TestisplayNa       d',
 e.comest@exampl   email: 't  
   d',t-user-i'tesd:       ui = {
  serkU  const moc> {
    c () =s', asyne credith welcomwitile proftes user t('crea> {
    iile', () =erProf('createUsibecr des
 p()
  })
eanu  test.cl
  (() => {
  afterAlluth)
  })
ckAmoValue(Returnin.auth.mockadmtore)
    Fireslue(mockockReturnVae.mmin.firestor    ad}


    jest.fn(),serClaims: CustomU     set,
 .fn() jestr:  getUse     {
Auth =ck
    mo}
    })),
    
       jest.fn(),     add: })),
   ),
       : jest.fn(date       up),
   fn(est.      get: j
    : jest.fn(),         set
 n(() => ({ jest.f      doc: => ({
  : jest.fn(()  collection= {
    ckFirestore 
    moeAll(() => { beforh

 let mockAutrestore
  et mockFi  l, () => {
nctions'h Fuutcribe('A

desc/auth')uire('../srle } = reqserProfipdateUProfile, uteUser{ creat 
consr mockingftenctions a Import fu//))

 })),
}
  jest.fn(),Claims:mUser setCusto.fn(),
   r: jest    getUse{
(() => (: jest.fn)),
  auth }
    })),
 t.fn(), jes
      add:      })),t.fn(),
jespdate:        u),
 .fn(et: jest   gfn(),
      set: jest.
       > ({est.fn(() = jc:   do=> ({
   .fn(() on: jestllecti    co) => ({
n((t.fstore: jes,
  fire()t.fn: jesAppizenitial ({
  i, () =>e-admin'rebas('fi.mockdmin
jestrebase A Fi)

// Mockadmin'e-ebasrequire('firmin = onst ad-test')()
c-functions('firebasere= requist nst teco